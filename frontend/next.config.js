/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  // For example, if you were using external images:
  // images: {
  //   domains: ['example.com'],
  // },
};

module.exports = nextConfig;
